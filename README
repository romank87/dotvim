Installation:
    git clone https://github.com/zathras/dotvim.git ~/.vim
    cd ~/.vim
    git submodule init
    git submodule update

Create symlinks:
    ln -s ~/.vim/vimrc ~/.vimrc

Install plugins:
    Open vim
    :BundleInstall in normal mode
